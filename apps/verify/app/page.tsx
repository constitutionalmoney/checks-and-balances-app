import { ApplicationShell } from "@cbc/ui";

export default function VerifyPage() {
  return (
    <ApplicationShell
      title="Public verifier shell"
      purpose="Reserved for a future anti-enumeration, participant-presented verification flow. No status lookup is available."
    />
  );
}
