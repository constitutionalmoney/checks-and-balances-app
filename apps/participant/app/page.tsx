import { ApplicationShell } from "@cbc/ui";

export default function ParticipantPage() {
  return (
    <ApplicationShell
      title="Participant application shell"
      purpose="Reserved for a future privacy-minimized participant experience after its dependent issues and policies are approved."
    />
  );
}
