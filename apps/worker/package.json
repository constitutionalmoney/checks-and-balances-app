{
  "name": "@cbc/worker",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ worker and readiness shell with no jobs or protocol behaviour",
  "license": "Apache-2.0",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cbc/config": "workspace:*",
    "@cbc/db": "workspace:*",
    "@cbc/verus": "workspace:*",
    "bullmq": "6.0.8",
    "fastify": "5.10.0"
  }
}
