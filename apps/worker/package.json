{
  "name": "@cbc/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable private VRSCTEST outbox worker and readiness service",
  "license": "Apache-2.0",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cbc/config": "workspace:*",
    "@cbc/db": "workspace:*",
    "@cbc/verus": "workspace:*",
    "bullmq": "6.0.8",
    "fastify": "5.10.0"
  }
}
