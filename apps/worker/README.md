# Worker shell

Owner: `@constitutionalmoney`

Checks PostgreSQL, Redis/BullMQ, and fake/private Verus RPC readiness and exposes health endpoints.
It registers no jobs and performs no notification, expiry, participant, wallet, chain, or protocol
work.
