# Deployment placeholder

Owner: `@constitutionalmoney`

Deployment is intentionally absent from WP-01. Issue #26 owns isolated testnet deployment,
secrets, TLS, backup, rollback, release approval, and runtime status controls. Mainnet remains
blocked.
