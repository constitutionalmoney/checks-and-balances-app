# `@cbc/db`

Owner: `@constitutionalmoney`

Provides a model-free Prisma schema plus PostgreSQL and Redis readiness checks. Protocol entities,
migrations, audit records, outbox records, and repositories are intentionally deferred to issue
#16. The seed command confirms the synthetic-only convention and writes no records.
