{
  "name": "@cbc/verus",
  "version": "0.0.0",
  "private": true,
  "description": "Readiness-only fake/VRSCTEST boundary with no write methods",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
