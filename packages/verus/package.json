{
  "name": "@cbc/verus",
  "version": "0.0.0",
  "private": true,
  "description": "Private typed VRSCTEST RPC adapter, payload guards, and deterministic fake",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src"
  }
}
