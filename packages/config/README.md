# `@cbc/config`

Owner: `@constitutionalmoney`

Validates local, CI, VRSCTEST, pilot, and reserved production runtime profiles. WP-01
keeps production, mainnet, public sessions, document upload, and every unfinished protocol
capability disabled.
