{
  "name": "@cbc/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Structured JSON logging with default sensitive-field redaction",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "10.3.1"
  }
}
