{
  "name": "@cbc/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Authentication boundary shell with distinct application audiences",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
