{
  "name": "@cbc/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Reserved framework-independent protocol domain boundary",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
